import { Injectable } from '@angular/core'; import { HttpClient } from '@angular/common/http'; @Injectable({providedIn:'root'}) export class EnrollmentService { private ids:number[]=[]; constructor(private http:HttpClient){} enroll(id:number){if(!this.ids.includes(id))this.ids.push(id)} unenroll(id:number){this.ids=this.ids.filter(x=>x!==id)} isEnrolled(id:number){return this.ids.includes(id)} getEnrolledIds(){return this.ids} getStudentsByCourse(id:number){return this.http.get<any[]>(`http://localhost:3000/students?courseId=${id}`)} }