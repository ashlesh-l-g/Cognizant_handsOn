import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { ActivatedRoute,Router } from '@angular/router'; import { Store } from '@ngrx/store'; import { Observable } from 'rxjs'; import { Course } from '../../models/course.model'; import { CourseCardComponent } from '../../components/course-card/course-card.component'; import { HighlightDirective } from '../../directives/highlight.directive'; import { loadCourses } from '../../store/course/course.actions'; import { selectAllCourses,selectCoursesError,selectCoursesLoading } from '../../store/course/course.selectors';
@Component({selector:'app-course-list',standalone:true,imports:[CommonModule,CourseCardComponent,HighlightDirective],template:`<h2>Courses</h2><input #q [value]="searchTerm"><button (click)="search(q.value)">Search</button><p *ngIf="loading$|async">Loading courses...</p><p class="error">{{error$|async}}</p><div class="grid"><app-course-card *ngFor="let c of courses$|async;trackBy:trackByCourseId" appHighlight="lightblue" [course]="c" (dblclick)="open(c.id)" (enrollRequested)="onEnroll($event)"/></div><p *ngIf="selectedCourseId">Selected course ID: {{selectedCourseId}}</p>`}) export class CourseListComponent implements OnInit{courses$:Observable<Course[]>;loading$;error$;selectedCourseId?:number;searchTerm='';constructor(private store:Store,private router:Router,private route:ActivatedRoute){this.courses$=store.select(selectAllCourses);this.loading$=store.select(selectCoursesLoading);this.error$=store.select(selectCoursesError)}ngOnInit(){this.searchTerm=this.route.snapshot.queryParamMap.get('search')||'';this.store.dispatch(loadCourses())}trackByCourseId(_:number,c:Course){return c.id}onEnroll(id:number){console.log('Enrolling in course: '+id);this.selectedCourseId=id}open(id:number){this.router.navigate(['courses',id])}search(v:string){this.router.navigate(['courses'],{queryParams:{search:v}})}}