import { Component } from '@angular/core'; import { Store } from '@ngrx/store'; import { AsyncPipe,JsonPipe } from '@angular/common'; import { selectEnrolledCourses } from '../../store/enrollment/enrollment.selectors'; @Component({standalone:true,imports:[AsyncPipe,JsonPipe],template:`<h2>Student Profile</h2><p>Enrolled courses:</p><pre>{{enrolled$|async|json}}</pre>`}) export class StudentProfileComponent{enrolled$=this.store.select(selectEnrolledCourses);constructor(private store:Store){}}