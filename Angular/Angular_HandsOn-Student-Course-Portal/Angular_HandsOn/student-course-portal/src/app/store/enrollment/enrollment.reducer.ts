import { createReducer,on } from '@ngrx/store'; import * as A from './enrollment.actions'; export interface EnrollmentState{enrolledCourseIds:number[]} export const enrollmentReducer=createReducer({enrolledCourseIds:[]} as EnrollmentState,on(A.enrollInCourse,(s,{courseId})=>({...s,enrolledCourseIds:[...new Set([...s.enrolledCourseIds,courseId])]})),on(A.unenrollFromCourse,(s,{courseId})=>({...s,enrolledCourseIds:s.enrolledCourseIds.filter(x=>x!==courseId)})),on(A.setEnrolledCourses,(s,{courseIds})=>({...s,enrolledCourseIds:courseIds})));