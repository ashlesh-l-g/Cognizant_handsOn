import { Component } from '@angular/core'; import { Store } from '@ngrx/store'; import { AsyncPipe } from '@angular/common'; import { selectAllCourses } from '../../store/course/course.selectors'; @Component({selector:'app-course-summary-widget',standalone:true,imports:[AsyncPipe],template:`<p>Total courses: {{(courses$|async)?.length}}</p>`}) export class CourseSummaryWidgetComponent{courses$=this.store.select(selectAllCourses);constructor(private store:Store){}}