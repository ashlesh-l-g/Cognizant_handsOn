import { Component } from '@angular/core'; import { NotificationService } from '../../services/notification.service'; @Component({selector:'app-notification',standalone:true,providers:[NotificationService],template:`<p>Notifications ready</p>`}) export class NotificationComponent { /* Component provider creates an isolated service instance for this component tree. */ constructor(public notifications:NotificationService){} }